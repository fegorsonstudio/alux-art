# Cron jobs to add on the VPS (replaces Vercel Cron)
# Edit with: crontab -e
#
# Cleanup expired shoots — runs every hour at :05
5 * * * * curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer YOUR_CRON_SECRET" https://aluxartandframes.shop/api/cron/cleanup-expired >> /var/log/aluxart-cron.log 2>&1
